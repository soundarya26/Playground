#include <stdio.h>

  int great(int n,int m,int k)
  {int big,big1;
    if(n>m){
      return big=n;}
    else {
      return big=m;
    }    if(big>k){
      return big1=big;}
    else{
      return big1=k;}
    return big1;
  }
int main()
    {
      int k,w,r,so;
      scanf("%d %d %d",&k,&w,&r);
      so=great(k,w,r);
      printf("%d",so);
    
    
    
    
    
	// Type your code here
  	return 0;
}